# b04 的 QM 复核：PRRS 找到的 TS 经 QM 精化 + IRC 确认

2026-09-04 · ORCA 6.1.1 on kasuga02 · r2SCAN-3c / TightSCF / 32 核 · 总电荷 −1

**起点是 PRRS 自己找到的 TS**（`runs/b04_probe2_openceiling/ts_candidates/ts0000.extxyz`，
在 MACE-POLAR-1-M 面上，`min_mode_following`，解析 Hessian order 1）。
链条与 P1 同一协议：`OptTS + Freq` → 双向 IRC → 两端 `OPT + Freq`。

## 结果

| | r(C–Cl) a | r(C–Cl) b | 虚频 |
| --- | --- | --- | --- |
| PRRS ts0000（POLAR-1 面上） | 2.2805 | 2.2827 | −535.65 |
| **QM 精化后（从上一行出发）** | **2.3645** | **2.3645** | **−314.41** |
| 先前独立跑的 `T[E_QM]` D3h | 2.3645 | 2.3645 | −314.42 |
| IRC 正向端点 | 1.8878 | 3.0376 | 0（真极小） |
| IRC 反向端点 | 3.0375 | 1.8879 | 0（真极小） |

**从 PRRS 的 TS 出发做 QM 精化，落到与先前独立求得的 QM 鞍点小数点后四位相同的位置。**
虚频 −314.41 对 −314.42。

IRC 两端互为**精确镜像**，且对上 `../sn2_qm/` 那次的离子-偶极复合物
（r(C–Cl) 1.8873、r(C···Cl⁻) 3.0357）。两端各 0 虚频，都是真极小。

## 通道确认不靠 key（退化反应两端 key 本来相同）

```
两端 chemical_key   eeccc12ca351f92c  /  eeccc12ca351f92c
QM    broken [[0,5]]  formed [[0,1]]    event_key 847306d3f8c57bd4
PRRS  broken [[0,1]]  formed [[0,5]]    event_key 847306d3f8c57bd4
```

同一个事件，两个方向 —— `event_key` 是方向商掉的，所以**逐字一致**。
原子 1 与 5 是两个氯，0 是碳：**碳从一个氯换到另一个氯。**

**QM 独立确认了 PRRS 在 POLAR-1 面上找到的反应事件身份。**

## 归因

| | |
| --- | --- |
| **方法** | PRRS 不被告知答案，找到的 TS 精化后就是 QM 的那个鞍点 |
| **模型** | POLAR-1 的鞍点比 QM 的紧 **0.083 Å**，虚频 −535.65 对 −314.41 |

与 P1 同一形状：**几何与事件身份属于方法且正确，能量与曲率属于模型。**

## 势能面记账

TS 精化、IRC、端点优化全部在 **r2SCAN-3c** 面上。
上表的 `chemical_key` / `event_key` 是在**那些几何**上算的。
PRRS 那一行在 **MACE-POLAR-1-M** 面上。两者分开记，不合并成一个数。

### 能量：ωB97M-V/def2-TZVPD 单点（已补）

r2SCAN-3c 的 def2-mTZVPP 无弥散函数而这是阴离子，所以能量必须补单点，
协议同 `../sn2_qm/`。在上面那组 r2SCAN-3c 几何上：

| | complex | TS vs ref | **TS vs complex** |
| --- | --- | --- | --- |
| 本次（PRRS → QM 精化的几何） | −11.63 | +1.39 | **+13.02** |
| `../sn2_qm/`（独立求得的几何） | −9.63 | +3.39 | **+13.02** |
| 文献（未核验） | −10.50 | +3.00 | +13.50 |
| MACE-POLAR-1-M（同几何单点） | — | — | +14.07 |

**中心势垒 +13.02 kcal/mol，两组独立几何逐位相同。**

前两列差约 2 kcal，原因是本次的参考态是把 Cl⁻ 从复合物几何拉到 20 Å、
**没有重新松弛 CH₃Cl**，而 `../sn2_qm/` 用的是单独优化过的 CH₃Cl。
参考态整体偏高，complex 与 TS_vs_ref 同向平移了同一个量，
**差值不受影响** —— 这正是中心势垒对得上的原因。
引用时用 **TS vs complex**，那一列不依赖参考态的构造方式。

## 成本

TS 优化 + 频率 **1 分 29 秒**（32 核，6 原子）。全链（含 IRC 与两端优化）数分钟。

原始输入输出与 IRC 轨迹在本目录。
