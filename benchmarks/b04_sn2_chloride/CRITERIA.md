# b04 判据 — **尚未冻结**

冻结是用户的行为，不是我的。这份文件现在只有流程，没有判据。

## 冻结的前提（全部未满足）

1. `preflight.py` 的**全部必需 gate 转绿**。当前 2/4，阻塞两条，见 `preflight.md`。
   在此之前写下的每一条判据都会被域门拒绝答复，而 `../README.md` 规矩 4
   要求把"被门拒了"和"没找到"分开记 —— 那样的基准只会报出一列门拒绝。
2. **b03 收口**（用户 2026-09-04 定）。
3. `config.json` 与 `model.json` 同时冻结。
   判据钉在一个未冻结的配置上没有意义，模型未选定时同理。

## 冻结时必须遵守的

- **只写这一格声明负责的量。** `../README.md` 规矩 2：
  PRRS 只负责 TS 在哪里、产物是什么。势垒和虚频**不进判据** ——
  两个候选模型在这个反应上过渡区能量差 2.9 倍、最低本征值差 12.8 倍
  （`reference.json`；波数写法下是 √12.83 = 3.58 倍，同一个事实）。
  写进去就是把模型的分歧记到方法头上。
  （那两个数**本身**也不能按字面读：D3h 点的残余梯度从未测量，
  所以它们既不是势垒也不是频率 —— 见 `../README.md` 规矩 6。）
- **机理标签只能是输出。** `../README.md` 规矩 5。这一格的核心问法是：

  > 取代通道上是否存在 `confirm_minimum` 通过的极小，搜索必须给出答复。
  > 存在 ⇒ 记为分步；不存在 ⇒ 记为协同。**两者都是 pass，答不出来才是 fail。**

  教科书说 SN2 是协同的。那句话在 `reference.json` 里，作为一条可被本基准
  证伪的预期，**不在这里**。
- **逐字冻结，改走修订记录。** 格式抄 `docs/P2_OXOBUTANAL_ACCEPTANCE.md`
  和 `docs/P2_TEMPERATURE_SCAN_PROTOCOL.md`：旧文本原样保留、改的理由、
  以及**修订发生在看到哪些数据之后**。

## 修订记录

（空）
